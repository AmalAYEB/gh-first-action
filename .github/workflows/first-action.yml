# workflow name
name: first workflow

# workflow trigger
on: workflow_dispatch

#jobs
jobs:
 first-job:
 #the environment in wich the diffrent steps will be executed
  runs-on: ubuntu-latest
  steps:
    - name: Print greeting
      run: echo "Hello World!"
    - name: Print goodbye
      run: echo "Done - bye!"
